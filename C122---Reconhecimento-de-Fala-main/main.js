var screenWidth = 0;
var screenHeight = 0;

var apple = "";

var speechData = "";
var toNumber = "";

function preload = "loadImage()";

to_number = Number(content);
if(Number.isInteger(to_number));